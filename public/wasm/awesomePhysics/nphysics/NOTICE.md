# nphysics2d WASM Pilot Notices

Preserve this notice with `nphysics2d_worker_probe.wasm` and
`nphysics2d_worker_probe.js`. The source-side build notice is retained at
`scripts/awesomePhysics/wasm/nphysics/NOTICE.md`.

This is a narrow headless `nphysics2d` object-form artifact built from
`awesome-physics-repos/nphysics` revision
`65aa85c5470a5da85e0c13652ce58400ae2e2201`. The selected `nphysics2d` crate
and `ncollide2d` `0.31.0` are Apache-2.0; `nalgebra` `0.28.0` is BSD-3-Clause;
`generational-arena` `0.2.9` is MPL-2.0; and `slotmap` `1.1.1` is Zlib.

The complete direct/transitive license inventory retained by the verified build
also includes the following MIT/Apache-2.0, BSD-3-Clause, and Unicode-3.0
terms: `approx` 0.5.1, `autocfg` 1.5.1, `bitflags` 1.3.2, `bumpalo` 3.20.3,
`cfg-if` 1.0.4, `downcast-rs` 1.2.1, `either` 1.17.0, `fixedbitset` 0.2.0,
`futures-core` 0.3.34, `futures-task` 0.3.34, `futures-util` 0.3.34,
`hashbrown` 0.12.3, `indexmap` 1.9.3, `instant` 0.1.13, `js-sys` 0.3.104,
`lazy_static` 1.5.0, `matrixmultiply` 0.3.11, `nalgebra-macros` 0.1.0,
`num-complex` 0.4.6, `num-integer` 0.1.47, `num-rational` 0.4.2,
`num-traits` 0.2.19, `once_cell` 1.21.4, `paste` 1.0.15, `petgraph` 0.5.1,
`pin-project-lite` 0.2.17, `proc-macro2` 1.0.107, `quote` 1.0.47,
`rawpointer` 0.2.1, `rustversion` 1.0.23, `simba` 0.5.1, `slab` 0.4.12,
`smallvec` 1.15.2, `syn` 1.0.109 and 2.0.119, `typenum` 1.20.1,
`unicode-ident` 1.0.24, `version_check` 0.9.5, `wasm-bindgen` 0.2.127,
`wasm-bindgen-macro` 0.2.127, `wasm-bindgen-macro-support` 0.2.127,
`wasm-bindgen-shared` 0.2.127, and `web-sys` 0.3.104. Exact expressions are
recorded in the source-side notice and retained Cargo.lock.

The source Apache-2.0 license and attribution must remain available with this
artifact. This legacy source is passively maintained and superseded by Rapier.
The finite fixture is not validation of a physical theory, model, or
experimental result. The pilot contains no nphysics3d artifact or claim.
