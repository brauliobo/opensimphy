# Vue 3.6.0-rc.9 is a prerelease. vue-router@4.6.4 declares peer vue@^3.5.0,
# which npm does not treat as matching 3.6.0-rc.*. The APIs are compatible;
# this only relaxes the peer check until Vue 3.6 stable or a router peer bump.
legacy-peer-deps=true
