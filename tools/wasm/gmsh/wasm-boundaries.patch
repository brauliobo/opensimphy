diff --git a/src/common/OS.cpp b/src/common/OS.cpp
index a48a38cfc..78f5ea7a6 100644
--- a/src/common/OS.cpp
+++ b/src/common/OS.cpp
@@ -600 +600 @@
-#elif(BUILD_IOS)
+#elif defined(BUILD_IOS) || defined(__EMSCRIPTEN__)
diff --git a/src/common/GmshSocket.h b/src/common/GmshSocket.h
index a5a886448..78a2aa755 100644
--- a/src/common/GmshSocket.h
+++ b/src/common/GmshSocket.h
@@ -102,5 +102,9 @@ protected:
   // send some data over the socket
   int _sendData(const void *buffer, int bytes)
   {
+#if defined(__EMSCRIPTEN__)
+    (void)buffer;
+    return bytes ? -1 : 0;
+#else
     const char *buf = (const char *)buffer;
     long int sofar = 0;
@@ -114,9 +118,14 @@ protected:
     } while(remaining > 0);
     _sent += bytes;
     return bytes;
+#endif
   }
   // receive some data over the socket
   int _receiveData(void *buffer, int bytes)
   {
+#if defined(__EMSCRIPTEN__)
+    (void)buffer;
+    return bytes ? -1 : 0;
+#else
     char *buf = (char *)buffer;
     long int sofar = 0;
@@ -130,4 +139,5 @@ protected:
     } while(remaining > 0);
     _received += bytes;
     return bytes;
+#endif
   }
@@ -250,6 +260,8 @@ public:
   void ShutdownSocket(int s)
   {
-#if !defined(WIN32) || defined(__CYGWIN__)
+#if defined(__EMSCRIPTEN__)
+    (void)s;
+#elif !defined(WIN32) || defined(__CYGWIN__)
     shutdown(s, SHUT_RDWR);
 #endif
   }
@@ -264,5 +264,9 @@ public:
   ~GmshClient() {}
   int Connect(const char *sockname)
   {
+#if defined(__EMSCRIPTEN__)
+    (void)sockname;
+    return -1;
+#else
     if(strstr(sockname, "/") || strstr(sockname, "\\") ||
        !strstr(sockname, ":")) {
@@ -320,3 +324,4 @@ public:
     CloseSocket(_sock);
     return -2; // couldn't connect
+#endif
   }
@@ -350,4 +355,11 @@ public:
   int Start(const std::string &exe, const std::string &args,
             const std::string &sockname, double timeout)
   {
+#if defined(__EMSCRIPTEN__)
+    (void)exe;
+    (void)args;
+    (void)sockname;
+    (void)timeout;
+    throw std::runtime_error("Sockets are unavailable in WebAssembly");
+#else
     _sockname = sockname;
@@ -460,3 +472,4 @@ public:
     if(_sock < 0) throw std::runtime_error("Socket accept failed");
     return _sock;
+#endif
   }
diff --git a/src/common/onelab.h b/src/common/onelab.h
index f54eac4b4..ed4f18392 100644
--- a/src/common/onelab.h
+++ b/src/common/onelab.h
@@ -1733,6 +1733,8 @@ namespace onelab {
     void runNonBlockingSubClient(const std::string &name,
                                  const std::string &command)
     {
-#if !defined(BUILD_IOS)
+#if defined(__EMSCRIPTEN__)
+      return;
+#elif !defined(BUILD_IOS)
       if(!_gmshClient) {
         int res = system(command.c_str());
diff --git a/src/mesh/Field.cpp b/src/mesh/Field.cpp
index 20e6611b9..3a3df3c99 100644
--- a/src/mesh/Field.cpp
+++ b/src/mesh/Field.cpp
@@ -1269 +1269,15 @@
-#if defined(WIN32) && !defined(__CYGWIN__)
+#if defined(__EMSCRIPTEN__)
+class Popen2 {
+public:
+  void stop() {}
+  bool started() const { return false; }
+  bool start(const char *command)
+  {
+    (void)command;
+    return false;
+  }
+  bool read(void *data, size_t size) { return false; }
+  bool write(void *data, size_t size) { return false; }
+};
+
+#elif defined(WIN32) && !defined(__CYGWIN__)
