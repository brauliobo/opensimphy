# surgery-floquet-tls

Program: `EARTH-FLD-007` / `traditional-numerical-baseline-v1`.
`validatesEarthTheory` stays `false`. Claim `FLD-007-FLOQ` is **blocked** as EARTH physics; the Shirley product is a software comparator.

## Sources

- **EARTH:** `EARTH/A Full Publication/Theorem/Topological Surgery Barrier.md` — `V=-½ω_b²x²+F x cos(ω_d t)`, N=201, `B≈628.318…`, Schumann `f=7.83 Hz`.
- **Thad:** `absent` — Physics Monastery / 288 Formula Atlas has no Floquet surgery-barrier operator.
- **Nassim:** `absent` — downloaded Haramein PDFs have no Floquet surgery-barrier operator.
- **SM:** Shirley (1965) two-level Floquet, already `floquetBenchmark`. Unitarity residual `<1e-11` is a software gate, not a surgery-barrier proof.

## Discrepancy

2-level ≠ N=201. `B=2π×100` after scaling the barrier to 100 v9.3 units is arithmetic-cx, not a surgery energy in J. Printed 7.83 Hz is not the dimensionless Shirley drive and is not a particle measurement.
